# Physics module - replaces the various "module XML files" in Underworld:

# Setup simply imports all the definitions files which are bulky strings of json dictionary stuff

from _mesh_setup import *
